<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Reminder</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1>Care connect</h1>
    <nav>
      <a href="/reminders">Reminders</a> |
      <a href="/logout">Logout</a>
    </nav>
  </header>
  <main>
    <h2>Edit Reminder</h2>
    <form action="/reminders/edit/<%= reminder.id %>" method="POST">
      <div>
        <label>Medication:</label>
        <input type="text" name="medication" value="<%= reminder.medication %>" required>
      </div>
      <div>
        <label>Schedule (HH:mm):</label>
        <input type="text" name="schedule" value="<%= reminder.schedule %>" required>
      </div>
      <div>
        <label>Message:</label>
        <input type="text" name="message" value="<%= reminder.message %>" required>
      </div>
      <button type="submit">Update Reminder</button>
    </form>
  </main>
</body>
</html>
